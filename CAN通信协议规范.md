# CAN通信协议规范

> 本文档定义了灵巧手控制系统中CAN总线通信的完整协议规范，包括帧格式、命令集、时序要求及错误处理机制。
> 适用于主控板（STM32F407）与各手指节点之间的分布式通信。

---

## 一、协议概述

### 1.1 基本参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 协议标准 | CAN 2.0B 标准帧 | 11位ID |
| 波特率 | 1 Mbps | 42MHz / (3 × 14) = 1Mbps |
| 帧类型 | 数据帧 | 不使用远程帧 |
| DLC | 固定8字节 | 所有帧统一8字节，未用字段填0x00 |
| 最大节点数 | 6 | 1个主控 + 5个手指节点 |
| 位时间配置 | Prop_Seg=2, Phase_Seg1=7, Phase_Seg2=4 | BS1=9, BS2=4, 总TQ=14, SJW=1, 采样点69% |

### 1.2 总线拓扑

```
                    ┌──────────────┐
                    │   主控板      │
                    │ (STM32F407)  │
                    │   ID: 0x00   │
                    └──────┬───────┘
                           │ CAN_H / CAN_L
            ┌──────────────┼──────────────┐
            │              │              │
     ┌──────┴──────┐ ┌────┴─────┐ ┌──────┴──────┐
     │  拇指节点    │ │ 食指节点  │ │  中指节点    │  ...
     │  ID: 0x01   │ │ ID: 0x02 │ │  ID: 0x03   │
     │  + FSR      │ │ + FSR    │ │  + FSR      │
     │  + 电机     │ │ + 电机   │ │  + 电机     │
     │  + 编码器   │ │ + 编码器 │ │  + 编码器   │
     └─────────────┘ └──────────┘ └─────────────┘
     [120Ω终端电阻]                            [120Ω终端电阻]
```

---

## 二、帧格式定义

### 2.1 CAN ID 分配规则

11位CAN ID按以下规则编码：

```
Bit 10~8:  优先级 (3bit, 0=最高优先级)
Bit 7~4:   目标节点ID (4bit, 0x0=广播, 0x1~0x5=手指节点)
Bit 3~0:   源节点ID (4bit, 0x0=主控, 0x1~0x5=手指节点)
```

#### 标准ID分配表

| CAN ID (11bit) | 十六进制 | 优先级 | 目标 | 源 | 用途 |
|----------------|---------|--------|------|-----|------|
| 0b_000_0001_0000 | 0x010 | 0 | 0x01(拇指) | 0x00(主控) | 主控→拇指 |
| 0b_000_0010_0000 | 0x020 | 0 | 0x02(食指) | 0x00(主控) | 主控→食指 |
| 0b_000_0011_0000 | 0x030 | 0 | 0x03(中指) | 0x00(主控) | 主控→中指 |
| 0b_000_0100_0000 | 0x040 | 0 | 0x04(无名指) | 0x00(主控) | 主控→无名指 |
| 0b_000_0101_0000 | 0x050 | 0 | 0x05(小指) | 0x00(主控) | 主控→小指 |
| 0b_000_0000_0001 | 0x001 | 0 | 0x00(主控) | 0x01(拇指) | 拇指→主控 |
| 0b_000_0000_0010 | 0x002 | 0 | 0x00(主控) | 0x02(食指) | 食指→主控 |
| 0b_000_0000_0011 | 0x003 | 0 | 0x00(主控) | 0x03(中指) | 中指→主控 |
| 0b_000_0000_0100 | 0x004 | 0 | 0x00(主控) | 0x04(无名指) | 无名指→主控 |
| 0b_000_0000_0101 | 0x005 | 0 | 0x00(主控) | 0x05(小指) | 小指→主控 |
| 0b_000_1111_0000 | 0x0F0 | 0 | 0x0F(广播) | 0x00(主控) | 广播(急停等) |

### 2.2 数据帧格式

所有帧统一使用8字节数据字段：

```
┌───────┬───────┬───────┬───────────────────────────────┐
│ Byte0 │ Byte1 │ Byte2~7 │                              │
│ CMD   │ SEQ   │ DATA    │                              │
│ 1B    │ 1B    │ 6B      │                              │
└───────┴───────┴─────────┴──────────────────────────────┘
```

| 字段 | 偏移 | 长度 | 说明 |
|------|------|------|------|
| CMD | Byte0 | 1B | 命令码 |
| SEQ | Byte1 | 1B | 序列号 (0~255循环，用于匹配请求/响应) |
| DATA | Byte2~7 | 6B | 命令数据 (未用字段填0x00) |

### 2.3 命令码 (CMD) 编码规则

```
Bit 7: 方向标志 (0=主控→节点, 1=节点→主控)
Bit 6~0: 命令编号

主控→节点 CMD范围: 0x00~0x7F
节点→主控 CMD范围: 0x80~0xFF
特殊命令: 0xFE(心跳), 0xFF(急停)
```

---

## 三、完整命令集

### 3.1 主控→节点命令 (CMD: 0x00~0x7F)

#### CMD 0x01: 设置关节角度

**功能**：主控下发目标关节角度给指定手指节点

| 字段 | 偏移 | 长度 | 类型 | 说明 |
|------|------|------|------|------|
| CMD | 0 | 1B | uint8 | 固定 0x01 |
| SEQ | 1 | 1B | uint8 | 序列号 |
| ANGLE_H | 2 | 1B | uint8 | 目标角度高字节 |
| ANGLE_L | 3 | 1B | uint8 | 目标角度低字节 (单位: 0.1度, 范围: 0~1800) |
| SPEED_H | 4 | 1B | uint8 | 运动速度高字节 |
| SPEED_L | 5 | 1B | uint8 | 运动速度低字节 (单位: 度/秒, 范围: 0~1000) |
| RESERVED | 6~7 | 2B | — | 保留，填0x00 |

**示例**：设置拇指角度为90.0度，速度200度/秒
```
CAN ID: 0x010, DLC: 8, DATA: [0x01] [SEQ] [0x03] [0x84] [0x00] [0xC8] [0x00] [0x00]
                              CMD    SEQ   900>>8  900&0xFF 200>>8  200&0xFF
```

#### CMD 0x02: 查询力传感器

**功能**：主控请求指定手指节点回传FSR力传感器数据

| 字段 | 偏移 | 长度 | 类型 | 说明 |
|------|------|------|------|------|
| CMD | 0 | 1B | uint8 | 固定 0x02 |
| SEQ | 1 | 1B | uint8 | 序列号 |
| FINGER_ID | 2 | 1B | uint8 | 手指ID (0x01~0x05, 0x00=全部) |
| RESERVED | 3~7 | 5B | — | 保留，填0x00 |

**响应**：节点收到后回复 CMD 0x82

#### CMD 0x03: 设置PID参数

**功能**：主控在线调整指定手指节点的PID控制参数（用于调试和自适应）

| 字段 | 偏移 | 长度 | 类型 | 说明 |
|------|------|------|------|------|
| CMD | 0 | 1B | uint8 | 固定 0x03 |
| SEQ | 1 | 1B | uint8 | 序列号 |
| PID_ID | 2 | 1B | uint8 | PID控制器选择 (0=位置环, 1=力矩环) |
| KP | 3 | 1B | uint8 | Kp × 10 (范围: 0~255, 实际值0~25.5) |
| KI | 4 | 1B | uint8 | Ki × 100 (范围: 0~255, 实际值0~2.55) |
| KD | 5 | 1B | uint8 | Kd × 10 (范围: 0~255, 实际值0~25.5) |
| RESERVED | 6~7 | 2B | — | 保留，填0x00 |

**响应**：节点收到后回复 CMD 0x83 确认

#### CMD 0x10: 设置控制模式

**功能**：切换指定手指节点的控制模式

| 字段 | 偏移 | 长度 | 类型 | 说明 |
|------|------|------|------|------|
| CMD | 0 | 1B | uint8 | 固定 0x10 |
| SEQ | 1 | 1B | uint8 | 序列号 |
| MODE | 2 | 1B | uint8 | 控制模式 (见下表) |
| FORCE_TARGET | 3 | 1B | uint8 | 目标力值 (单位: 0.5N, 仅力矩模式有效) |
| RESERVED | 4~7 | 4B | — | 保留，填0x00 |

**控制模式定义**：

| MODE值 | 模式名称 | 说明 |
|--------|---------|------|
| 0x00 | 待机模式 | 电机失电，自由转动 |
| 0x01 | 位置模式 | 跟踪CMD 0x01下发的目标角度 |
| 0x02 | 力矩模式 | 恒力抓取，目标力由FORCE_TARGET指定 |
| 0x03 | 阻抗模式 | 力-位置混合控制 (高级功能) |
| 0x04 | 自动模式 | 自动切换位置/力矩 (默认模式) |

#### CMD 0x11: 查询编码器位置

**功能**：查询指定手指当前编码器位置

| 字段 | 偏移 | 长度 | 类型 | 说明 |
|------|------|------|------|------|
| CMD | 0 | 1B | uint8 | 固定 0x11 |
| SEQ | 1 | 1B | uint8 | 序列号 |
| RESERVED | 2~7 | 6B | — | 保留，填0x00 |

**响应**：节点回复 CMD 0x81

### 3.2 节点→主控命令 (CMD: 0x80~0xFD)

#### CMD 0x81: 角度回传

**功能**：节点回传当前关节角度

| 字段 | 偏移 | 长度 | 类型 | 说明 |
|------|------|------|------|------|
| CMD | 0 | 1B | uint8 | 固定 0x81 |
| SEQ | 1 | 1B | uint8 | 序列号 (与请求帧一致) |
| ANGLE_H | 2 | 1B | uint8 | 当前角度高字节 |
| ANGLE_L | 3 | 1B | uint8 | 当前角度低字节 (单位: 0.1度) |
| ENCODER_H | 4 | 1B | uint8 | 编码器原始值高字节 |
| ENCODER_L | 5 | 1B | uint8 | 编码器原始值低字节 |
| STATUS | 6 | 1B | uint8 | 状态标志 (见状态字节定义) |
| RESERVED | 7 | 1B | — | 保留 |

#### CMD 0x82: 力数据回传

**功能**：节点回传FSR力传感器数据

| 字段 | 偏移 | 长度 | 类型 | 说明 |
|------|------|------|------|------|
| CMD | 0 | 1B | uint8 | 固定 0x82 |
| SEQ | 1 | 1B | uint8 | 序列号 |
| FORCE_H | 2 | 1B | uint8 | 力值高字节 |
| FORCE_L | 3 | 1B | uint8 | 力值低字节 (单位: 0.01N, 范围: 0~10000) |
| ADC_H | 4 | 1B | uint8 | ADC原始值高字节 |
| ADC_L | 5 | 1B | uint8 | ADC原始值低字节 (12-bit) |
| STATUS | 6 | 1B | uint8 | 状态标志 |
| CONTACT | 7 | 1B | uint8 | 接触标志 (0=未接触, 1=接触) |

#### CMD 0x83: PID参数回传

**功能**：节点回传当前PID参数（用于确认设置）

| 字段 | 偏移 | 长度 | 类型 | 说明 |
|------|------|------|------|------|
| CMD | 0 | 1B | uint8 | 固定 0x83 |
| SEQ | 1 | 1B | uint8 | 序列号 |
| PID_ID | 2 | 1B | uint8 | PID控制器选择 |
| KP | 3 | 1B | uint8 | Kp × 10 |
| KI | 4 | 1B | uint8 | Ki × 100 |
| KD | 5 | 1B | uint8 | Kd × 10 |
| STATUS | 6 | 1B | uint8 | 状态标志 |
| RESERVED | 7 | 1B | — | 保留 |

### 3.3 特殊命令

#### CMD 0xFE: 心跳

**功能**：主控周期性广播心跳，各节点用于检测主控在线状态

| 字段 | 偏移 | 长度 | 类型 | 说明 |
|------|------|------|------|------|
| CMD | 0 | 1B | uint8 | 固定 0xFE |
| SEQ | 1 | 1B | uint8 | 序列号 (递增) |
| TIMESTAMP | 2~5 | 4B | uint32 | 主控系统时间戳 (ms) |
| STATUS | 6 | 1B | uint8 | 主控状态 (bit0=运行, bit1=急停) |
| NODE_MASK | 7 | 1B | uint8 | 期望在线节点位图 (bit1~bit5对应5根手指) |

**CAN ID**：0x0F0 (广播)

**发送周期**：100ms

**节点响应**：节点收到心跳后更新 last_heartbeat 时间戳，无需回复。若500ms未收到心跳，节点进入安全停机状态。

#### CMD 0xFF: 紧急停止

**功能**：主控广播紧急停止命令，所有节点立即停止电机

| 字段 | 偏移 | 长度 | 类型 | 说明 |
|------|------|------|------|------|
| CMD | 0 | 1B | uint8 | 固定 0xFF |
| SEQ | 1 | 1B | uint8 | 序列号 |
| REASON | 2 | 1B | uint8 | 急停原因 (见下表) |
| RESERVED | 3~7 | 5B | — | 保留，填0x00 |

**CAN ID**：0x0F0 (广播，最高优先级)

**急停原因码**：

| REASON值 | 说明 |
|---------|------|
| 0x00 | 未知原因 |
| 0x01 | 物理急停按钮按下 |
| 0x02 | 电机堵转检测 |
| 0x03 | CAN心跳超时 |
| 0x04 | 过流保护触发 |
| 0x05 | 关节超限 |
| 0x06 | 视觉系统离线 |
| 0x10 | 人工急停 (上位机) |

**节点响应**：收到急停命令后，节点在1ms内关闭所有电机PWM输出，设置错误标志，并回复确认帧（CMD=0x84）。

#### CMD 0x84: 急停确认

**功能**：节点向主控确认已执行急停

| 字段 | 偏移 | 长度 | 类型 | 说明 |
|------|------|------|------|------|
| CMD | 0 | 1B | uint8 | 固定 0x84 |
| SEQ | 1 | 1B | uint8 | 序列号 (与急停帧一致) |
| NODE_ID | 2 | 1B | uint8 | 节点ID |
| STATUS | 3 | 1B | uint8 | 确认状态 (0x01=已停机) |
| RESERVED | 4~7 | 4B | — | 保留 |

### 3.4 状态字节定义

回传帧中的STATUS字段各bit含义：

| Bit | 名称 | 说明 |
|-----|------|------|
| bit0 | RUN | 1=运行中, 0=停止 |
| bit1 | ESTOP | 1=急停激活 |
| bit2 | STALL | 1=电机堵转 |
| bit3 | OVERCURRENT | 1=过流保护 |
| bit4 | COMM_ERR | 1=通信错误 |
| bit5 | SENSOR_ERR | 1=传感器错误 |
| bit6 | LIMIT | 1=关节限位 |
| bit7 | CALIBRATED | 1=已标定 |

---

## 四、通信时序图

### 4.1 主控→节点的标准交互流程

```
主控板 (STM32F407)                    手指节点 (STM32)
        │                                   │
        │  ── CMD 0x01 (设置角度) ──→       │
        │     CAN ID: 0x010                 │
        │     SEQ: 0x01                     │
        │     DATA: [0x01,0x01,0x03,0x84,   │
        │            0x00,0xC8,0x00,0x00]   │
        │                                   │
        │     (节点执行电机运动)             │
        │                                   │
        │  ←── CMD 0x81 (角度回传) ──       │
        │     CAN ID: 0x001                 │
        │     SEQ: 0x01                     │
        │     DATA: [0x81,0x01,0x03,0x70,   │
        │            0x01,0x2C,0x03,0x00]   │
        │                                   │
        │  ── CMD 0x02 (查询力) ──→         │
        │     CAN ID: 0x010                 │
        │     SEQ: 0x02                     │
        │                                   │
        │  ←── CMD 0x82 (力数据) ──         │
        │     CAN ID: 0x001                 │
        │     SEQ: 0x02                     │
        │     DATA: [0x82,0x02,0x00,0x4B,   │
        │            0x03,0x20,0x01,0x01]   │
        │                                   │
```

### 4.2 心跳与在线检测时序

```
主控板                                    节点1    节点2    节点3    节点4    节点5
  │                                        │        │        │        │        │
  │── CMD 0xFE (广播心跳) ──────────→      │        │        │        │        │
  │   CAN ID: 0x0F0                        │        │        │        │        │
  │   每100ms发送一次                       │        │        │        │        │
  │                                        │        │        │        │        │
  │   (各节点更新last_heartbeat)            ●        ●        ●        ●        ●
  │                                        │        │        │        │        │
  │   ... 100ms ...                        │        │        │        │        │
  │                                        │        │        │        │        │
  │── CMD 0xFE (下一帧心跳) ────────→      │        │        │        │        │
  │                                        ●        ●        ●        ●        ●
  │                                        │        │        │        │        │
  │   ... 假设节点3离线 ...                 │        │        ✗        │        │
  │                                        │        │        │        │        │
  │── CMD 0xFE ────────────────────→       │        │        ✗        │        │
  │   NODE_MASK: 0b00011111 (全部期望)     ●        ●        ✗        ●        ●
  │                                        │        │        │        │        │
  │   (主控检测: 节点3的last_heartbeat      │        │        │        │        │
  │    超过500ms → 判定离线 → LED_ERR亮)    │        │        │        │        │
  │                                        │        │        │        │        │
```

### 4.3 紧急停止时序

```
主控板                                    所有节点
  │                                        │
  │   (检测到急停条件: 物理按钮/堵转/...)   │
  │                                        │
  │── CMD 0xFF (紧急停止) ─────────→       │
  │   CAN ID: 0x0F0 (最高优先级)           │
  │   REASON: 0x01 (物理按钮)              │
  │                                        │
  │   (所有节点在1ms内关闭PWM)             ●━━ 1ms ━━→ [PWM=0, 电机停止]
  │                                        │
  │  ←── CMD 0x84 (急停确认) ──            │
  │     节点1确认                           │
  │  ←── CMD 0x84 ──                       │
  │     节点2确认                           │
  │  ←── CMD 0x84 ──                       │
  │     ...                                 │
  │                                        │
  │   (主控收到全部确认 → LED_ERR常亮)      │
  │   (等待人工复位)                        │
```

---

## 五、错误处理机制

### 5.1 超时检测

| 超时类型 | 阈值 | 检测方 | 处理动作 |
|---------|------|--------|---------|
| 心跳超时 | 500ms | 节点检测主控心跳 | 节点进入安全停机，LED闪烁告警 |
| 命令响应超时 | 10ms | 主控等待节点回复 | 主控重发命令，最多重试3次 |
| 连续通信失败 | 5次 | 主控统计 | 标记该节点离线，LED_ERR亮 |

### 5.2 CRC错误处理

本协议使用CAN总线自带的CRC-15校验机制，无需在应用层额外添加CRC。当CAN控制器检测到CRC错误时：

1. CAN控制器自动丢弃错误帧
2. 错误帧计入REC/TEC计数器
3. 若连续出现CRC错误，说明总线物理层存在问题（干扰、接线松动等）
4. 固件记录CRC错误计数，超过阈值（100次/秒）时发出告警

### 5.3 节点离线检测

```c
// 主控板心跳管理逻辑
typedef struct {
    uint8_t node_id;
    uint32_t last_heartbeat_ack;  // 最后一次收到节点响应的时间
    uint8_t online;               // 在线标志
    uint8_t miss_count;           // 连续丢失计数
} NodeStatus_t;

#define NODE_TIMEOUT_MS    500    // 节点超时时间
#define MAX_MISS_COUNT     5      // 最大连续丢失次数

void CheckNodeOnline(NodeStatus_t *node) {
    if (HAL_GetTick() - node->last_heartbeat_ack > NODE_TIMEOUT_MS) {
        node->miss_count++;
        if (node->miss_count >= MAX_MISS_COUNT) {
            node->online = 0;  // 标记离线
            // 触发错误处理：停止对应手指电机
            Motor_Stop(node->node_id);
            LED_ERR_On();
        }
    } else {
        node->miss_count = 0;
        node->online = 1;
    }
}
```

### 5.4 错误恢复机制

| 错误状态 | 恢复条件 | 恢复动作 |
|---------|---------|---------|
| 节点离线 | 收到该节点有效帧 | 重新标记在线，清除错误标志 |
| 心跳超时 | 收到主控心跳帧 | 恢复正常运行 |
| 电机堵转 | 人工按下复位按钮 | 清除堵转标志，恢复待机 |
| 急停状态 | 人工按下复位按钮 + 重新发送CMD 0x10 | 恢复正常控制模式 |

### 5.5 总线错误处理

CAN控制器具有自动错误恢复机制：

| 错误类型 | CAN控制器行为 | 固件处理 |
|---------|-------------|---------|
| 位错误 | 自动重发 | 记录TEC++ |
| 格式错误 | 丢弃帧 | 记录REC++ |
| ACK错误 | 自动重发 | 检查节点是否在线 |
| CRC错误 | 丢弃帧 | 记录REC++ |
| 总线关闭 (TEC>255) | 停止发送 | 尝试CAN控制器复位恢复 |

---

## 六、节点ID分配规则

### 6.1 固定ID分配

| 节点ID | 手指 | CAN发送ID | CAN接收ID | 物理位置 |
|--------|------|----------|----------|---------|
| 0x01 | 拇指 (Thumb) | 0x001 | 0x010 | 外侧第一指 |
| 0x02 | 食指 (Index) | 0x002 | 0x020 | 第二指 |
| 0x03 | 中指 (Middle) | 0x003 | 0x030 | 第三指 |
| 0x04 | 无名指 (Ring) | 0x004 | 0x040 | 第四指 |
| 0x05 | 小指 (Pinky) | 0x005 | 0x050 | 内侧第五指 |

### 6.2 节点ID存储

节点ID通过以下方式确定（优先级从高到低）：

1. **硬件拨码开关**：节点板上4位拨码开关，直接读取GPIO电平
2. **Flash存储**：首次烧录时写入Flash，后续读取
3. **自动分配**：主控发送CMD 0x20（节点分配），节点回复确认

### 6.3 CAN过滤器配置

**主控板CAN过滤器**（接收所有节点的回传帧）：

```c
// 过滤器配置: 接收ID 0x001~0x005 (节点→主控)
CAN_FilterTypeDef filter;
filter.FilterBank = 0;
filter.FilterMode = CAN_FILTERMODE_IDMASK;
filter.FilterScale = CAN_FILTERSCALE_32BIT;
filter.FilterIdHigh = 0x0001 << 5;     // ID最小值
filter.FilterIdLow = 0x0000;
filter.FilterMaskIdHigh = 0x07FE << 5; // 掩码: 只匹配bit4~bit0
filter.FilterMaskIdLow = 0x0000;
filter.FilterActivation = ENABLE;
```

**节点板CAN过滤器**（只接收主控发给自己的帧 + 广播帧）：

```c
// 过滤器配置: 接收 ID=0x0XX (目标为自己) 或 ID=0x0F0 (广播)
CAN_FilterTypeDef filter;
filter.FilterBank = 0;
filter.FilterMode = CAN_FILTERMODE_IDLIST;
filter.FilterScale = CAN_FILTERSCALE_32BIT;
// 第一个ID: 主控→本节点 (如拇指=0x010)
filter.FilterIdHigh = (MY_RX_ID) << 5;
filter.FilterIdLow = 0x0000;
// 第二个ID: 广播帧 0x0F0
filter.FilterMaskIdHigh = (0x0F0) << 5;
filter.FilterMaskIdLow = 0x0000;
filter.FilterActivation = ENABLE;
```

---

## 附录A：协议版本历史

| 版本 | 日期 | 修改内容 |
|------|------|---------|
| v1.0 | 2026-06 | 初始版本，定义基础命令集 |

## 附录B：CAN总线负载估算

| 帧类型 | 发送频率 | 帧长度 | 单帧耗时 | 带宽占用 |
|--------|---------|--------|---------|---------|
| 心跳 (CMD 0xFE) | 10Hz | 8B | 0.128ms | 1.28% |
| 设置角度 (CMD 0x01) | 30Hz × 5节点 | 8B | 0.128ms | 19.2% |
| 角度回传 (CMD 0x81) | 30Hz × 5节点 | 8B | 0.128ms | 19.2% |
| 力查询 (CMD 0x02) | 10Hz × 5节点 | 8B | 0.128ms | 6.4% |
| 力回传 (CMD 0x82) | 10Hz × 5节点 | 8B | 0.128ms | 6.4% |
| **总负载** | | | | **~52.5%** |

> CAN总线建议负载不超过70%，当前设计负载约52.5%，留有充足余量。

## 附录C：典型应用场景帧序列

### C1. 正常手势跟踪

```
时间轴 (ms)
0    ── 主控发送CMD 0x10(设置自动模式) ──→ 各节点
1    ←── 各节点回复CMD 0x83 ──
10   ── 主控发送CMD 0x01(设置角度) ×5 ──→ 各节点 (轮询)
12   ←── 节点1回复CMD 0x81 ──
13   ←── 节点2回复CMD 0x81 ──
...
33   ── 主控发送CMD 0x02(查询力) ×5 ──→ 各节点
35   ←── 节点1回复CMD 0x82 ──
...
100  ── 主控发送CMD 0xFE(心跳) ──→ 广播
...  重复 ...
```

### C2. 抓取物体 (力闭环)

```
时间轴 (ms)
0    ── 主控CMD 0x01(合拢角度) ──→ 各节点
5    ←── 各节点角度回传 ──
10   ── 主控CMD 0x02(查询力) ──→ 各节点
12   ←── 节点回传: FORCE=2.5N, CONTACT=1 ──
15   ── 主控CMD 0x10(切换力矩模式, TARGET=5N) ──→ 各节点
20   ←── 节点确认CMD 0x83 ──
...  节点在本地以500Hz运行力矩PID ...
50   ── 主控CMD 0x02(查询力) ──→ 各节点
52   ←── 节点回传: FORCE=5.1N, CONTACT=1 (力稳定) ──
```
